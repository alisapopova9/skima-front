export default class Sprint {
    constructor(index, title, description, duration) {
        this.index = index;
        this.title = title;
        this.description = description;
        this.duration = duration;
    }
}